# using for loops
for (val in 1: 5)
{
    # statements to be executed
    print(val)
}

#iterating through items of a list
items<-c("hello","world","good","morning")

for (item in items)
{
    #using the item values inside loop
    print(item)
}
